dataContractSpecification: 0.9.2
id: orders-unit-test
info:
  title: Orders Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/local-delta/data/orders
    format: delta
    dataProductId: orders
models:
  orders:
    fields:
      order_id:
        type: varchar # this is not true
        unique: true
        required: true
      order_timestamp: #does not exist
        required: true
      order_total:
        type: bigint
        required: true
