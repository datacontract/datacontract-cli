dataContractSpecification: 1.1.0
id: bigint-unit-test
info:
  title: BigInt Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/bigint.parquet
    format: parquet
models:
  example:
    fields:
      bigint_field:
        type: bigint
