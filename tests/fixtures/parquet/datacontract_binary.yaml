dataContractSpecification: 1.1.0
id: blob-unit-test
info:
  title: Blob Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/blob.parquet
    format: parquet
models:
  blob:
    description: Test data with binary field
    type: table
    fields:
      blob_field:
        type: bytes
