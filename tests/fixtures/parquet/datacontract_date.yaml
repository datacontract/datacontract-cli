dataContractSpecification: 0.9.3
id: date-unit-test
info:
  title: Date Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/date.parquet
    format: parquet
models:
  date:
    fields:
      date_field:
        type: date