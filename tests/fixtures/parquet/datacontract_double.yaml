dataContractSpecification: 1.1.0
id: double-unit-test
info:
  title: Double Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/double.parquet
    format: parquet
models:
  example:
    fields:
      double_field:
        type: double
