dataContractSpecification: 0.9.3
id: interval-unit-test
info:
  title: Interval Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/interval.parquet
    format: parquet
models:
  example:
    fields:
      interval:
        type: interval