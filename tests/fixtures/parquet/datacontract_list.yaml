dataContractSpecification: 0.9.3
id: list-unit-test
info:
  title: List Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/list.parquet
    format: parquet
models:
  example:
    fields:
      element:
        type: list
        items:
          type: integer