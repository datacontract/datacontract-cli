dataContractSpecification: 1.1.0
id: map-unit-test
info:
  title: Map Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/map.parquet
    format: parquet
models:
  example:
    fields:
      map_field:
        type: map
        keys:
          type: string
        values:
          type: string
