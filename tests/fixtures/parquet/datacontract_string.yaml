dataContractSpecification: 1.1.0
id: string-unit-test
info:
  title: String Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/parquet/data/string.parquet
    format: parquet
models:
  example:
    fields:
      string_field:
        type: varchar
